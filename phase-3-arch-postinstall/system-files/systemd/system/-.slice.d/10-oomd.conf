# Activate systemd-oomd swap-pressure monitoring on the root slice.
# When SwapUsedLimit (oomd.conf) is exceeded, oomd picks the descendant cgroup
# with the highest swap usage and SIGKILLs it.

[Slice]
ManagedOOMSwap=kill
