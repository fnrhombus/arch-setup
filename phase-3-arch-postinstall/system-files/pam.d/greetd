#%PAM-1.0
# arch-setup: greetd PAM stack — fingerprint + password.
#
# Tricky to get right because pam_fprintd holds input focus during its
# wait — typing a password during that window is silently dropped.
# Two design choices keep this from biting:
#
#   1. SHORT TIMEOUT (5s, max-tries=1). pam_fprintd waits at most 5s
#      for a finger before timing out and releasing the input channel.
#      A user who knows they want password types and waits 5s; a user
#      who taps finger gets instant login. Total worst-case delay: 5s.
#
#   2. `[success=done default=ignore]` CONTROL. Replaces `sufficient`.
#      success → auth complete, stop here. Failure / timeout / any
#      other return → IGNORED (treated as "module had no opinion").
#      Critically, pam_faillock checks the OVERALL auth result, not
#      individual module returns — so an ignored fprintd failure
#      does NOT increment the failure counter that triggers the
#      10-minute lockout. Only failed PASSWORDS bump faillock.
#
# pam_gnome_keyring lines: auth + session + password are all needed.
#   - auth     optional  → passes the typed password to the keyring
#                          daemon via PAM_AUTHTOK so it can unlock
#                          ~/.local/share/keyrings/Default.keyring
#                          silently. WITHOUT this line the daemon
#                          starts (via session) but never gets the
#                          password, so apps prompt for it later.
#   - session  optional auto_start → starts gnome-keyring-daemon for
#                          the session.
#   - password optional → keeps the keyring password in sync if the
#                          user ever changes their UNIX password.
#
# Module name quirk: pinpam-git ships its module as libpinpam.so (not
# pam_pinpam.so). We do NOT include libpinpam at the greeter on purpose
# — PIN is not a greeter-tier auth method on this design.
#
# Installed to /etc/pam.d/greetd by phase-2-arch-install/chroot.sh AND
# re-stomped on every postinstall run by phase-3 §7a (drift correction).

auth        [success=done default=ignore] pam_fprintd.so max-tries=1 timeout=5
auth        include     system-login
auth        optional    pam_gnome_keyring.so

account     include     system-login

password    include     system-login
password    optional    pam_gnome_keyring.so

session     include     system-login
session     optional    pam_gnome_keyring.so auto_start
