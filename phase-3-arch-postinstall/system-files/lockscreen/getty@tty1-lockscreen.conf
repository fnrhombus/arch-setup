# Swap agetty for the lockscreen-tty wrapper on tty1 only.
# Empty ExecStart= clears the parent unit's value; the leading `-` keeps
# systemd from marking the unit failed if the wrapper exits non-zero.
[Service]
ExecStart=
ExecStart=-/usr/local/bin/lockscreen-tty
